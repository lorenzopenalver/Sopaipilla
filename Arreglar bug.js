export function solution(cars) {
    // 👇 Este es el código que no funciona
    return cars.find(function (car) {
      var CarName = car.filter(function (cars) {
        return cars.color
    });
    });
  }
  